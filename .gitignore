node_modules
dist

# Klucz do biegów żywych (`scenarios:live`, bieg eksploracyjny bez --offline).
# NIGDY nie commitować. Szablon: .env.example
.env.local
.env

# Deploy wtyczki-nosiciela (companion/) do vaulta dewelopera - ścieżka lokalna, nie sekret,
# ale specyficzna dla jednej maszyny. Szablon: companion/deploy.local.example.json
companion/deploy.local.json

.DS_Store
.vscode
